#usda 1.0

def Scope "Asset" (
    prepend apiSchemas = ["CollectionAPI:parts"]
)
{
    # Anchored to /Asset here; composition maps it into the referencing
    # prim's namespace.
    uniform pathExpression collection:parts:membershipExpression = ".//"

    def Scope "Bolt"
    {
    }
}
