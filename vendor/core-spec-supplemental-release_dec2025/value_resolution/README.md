# Value Resolution

This directory contains an experimental sample implementation of the USD value resolution, per the AOUSD Core Specification.

## Running Tests

To run all value resolution tests, run this from the repository root:

```
uv run -m unittest discover value_resolution
```