#usda 1.0
(
    framesPerSecond = 24
    framePrecision = 3
)

def Model "FergusCloak" (
    add variantSets = "standin"
)
{
    variantSet "standin" = {
        "anim" (
            references = @./FergusCloak_anim.usd@</FergusCloak>
        ) {

        }
        "layout" (
            references = @./FergusCloak_layout.usd@</FergusCloak>
        ) {

        }
        "render" (
            references = @./FergusCloak_render.usd@</FergusCloak>
        ) {

        }
        "sim" (
            references = @./FergusCloak_sim.usd@</FergusCloak>
        ) {

        }
    }
}
