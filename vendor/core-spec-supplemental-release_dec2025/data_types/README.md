# Composition

This directory contains a sample implementation of the USD foundational data types.

## Running Tests

To run all data_types tests, run this from the repository root:

`uv run -m unittest discover data_types`
